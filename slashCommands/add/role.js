const { MessageEmbed } = require('discord.js')
const { success, fail } = require('./../../functions/createEmbeds')
module.exports = async(interaction, config) => {
    //check for placeholder in row creation and remove it

}